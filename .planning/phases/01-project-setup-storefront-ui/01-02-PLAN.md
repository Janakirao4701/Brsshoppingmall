---
phase: 01-project-setup-storefront-ui
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [src/components/layout/Navbar.tsx, src/components/layout/Footer.tsx, src/components/layout/MobileTabBar.tsx, src/components/sections/HeroBanner.tsx, src/components/sections/StoreLocator.tsx, src/components/sections/WhatsAppFloat.tsx, src/app/page.tsx, src/app/layout.tsx]
autonomous: true
requirements: [STORE-01, STORE-02]

must_haves:
  truths:
    - "User can view a carousel hero banner with rotating slides on the homepage"
    - "User can see sticky navigation bar on desktop with BSR logo and category links"
    - "User can see fixed bottom tab bar with 4 tabs on mobile"
    - "User can interact with embedded Google Map showing Sompeta and Palasa branches"
    - "User can click WhatsApp floating button to open WhatsApp chat"
    - "User can click phone number to initiate a call"
  artifacts:
    - path: "src/components/layout/Navbar.tsx"
      provides: "Sticky top navigation with logo, categories, and action icons"
      contains: "sticky"
    - path: "src/components/layout/MobileTabBar.tsx"
      provides: "Fixed bottom tab bar for mobile (Home/Categories/Cart/Account)"
      contains: "fixed"
    - path: "src/components/sections/HeroBanner.tsx"
      provides: "Carousel hero banner with auto-play and swipe"
      contains: "Carousel"
    - path: "src/components/sections/StoreLocator.tsx"
      provides: "Branch locator with Google Maps iframe and contact details"
      contains: "iframe"
    - path: "src/components/sections/WhatsAppFloat.tsx"
      provides: "Floating WhatsApp CTA button"
      contains: "wa.me"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/layout/Navbar.tsx"
      via: "import in layout"
      pattern: "Navbar"
    - from: "src/app/page.tsx"
      to: "src/components/sections/HeroBanner.tsx"
      via: "import in homepage"
      pattern: "HeroBanner"
---

<objective>
Build the core homepage sections: Hero Banner carousel, sticky Navbar, mobile bottom tab bar, Store Locator with Google Maps, and floating WhatsApp button.

Purpose: Deliver the primary storefront experience that users see on first visit — the brand impression, store info, and contact access.
Output: Fully functional homepage with all STORE-01 and STORE-02 requirements satisfied.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-setup-storefront-ui/01-CONTEXT.md
@.planning/phases/01-project-setup-storefront-ui/01-RESEARCH.md
@.planning/phases/01-project-setup-storefront-ui/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Navbar and MobileTabBar layout components</name>
  <files>src/components/layout/Navbar.tsx, src/components/layout/MobileTabBar.tsx, src/components/layout/Footer.tsx, src/app/layout.tsx</files>
  <read_first>src/app/layout.tsx, src/components/ui/navigation-menu.tsx, src/components/ui/sheet.tsx, tailwind.config.ts</read_first>
  <action>
    1. Create `src/components/layout/Navbar.tsx` as a `'use client'` component:
       - `position: sticky; top: 0; z-index: 50` with backdrop-blur and border-bottom
       - Left: BSR Shopping Mall logo (text-based with brand gradient for now, image logo will be added when asset is available)
       - Center: Desktop navigation links — "Men", "Women", "Kids", "Bulk Orders" (hidden on mobile via `hidden md:flex`)
       - Right: Search icon button, Language dropdown placeholder (globe icon), Cart icon with badge, Account icon
       - Mobile: Hamburger menu icon (using Shadcn Sheet for slide-out drawer) with the same nav links
       - Use Shadcn `NavigationMenu` for desktop and `Sheet` for mobile drawer

    2. Create `src/components/layout/MobileTabBar.tsx` as a `'use client'` component:
       - `position: fixed; bottom: 0; z-index: 50` with safe-area padding (`pb-safe`)
       - 4 tabs: Home (House icon), Categories (Grid icon), Cart (ShoppingCart icon), Account (User icon)
       - Active tab highlighted with brand-red color
       - Only visible on mobile: `md:hidden`
       - Uses `lucide-react` icons: Home, LayoutGrid, ShoppingCart, User

    3. Create `src/components/layout/Footer.tsx`:
       - Brand name "BSR Shopping Mall - Baratam Group"
       - Contact: Phone +91 78293 33444, address for both branches
       - Hours: Daily 9:00 AM – 9:00 PM
       - Quick links: Men, Women, Kids, Bulk Orders, Contact
       - Social media placeholder links
       - Copyright: "© 2026 BSR Shopping Mall. All rights reserved."
       - Background: dark (slate-900), text white

    4. Update `src/app/layout.tsx` to import and render `<Navbar />` above `{children}`, `<Footer />` below, and `<MobileTabBar />` at the end of body. Add `pb-16 md:pb-0` padding to main content area to account for mobile tab bar.
  </action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - src/components/layout/Navbar.tsx contains "sticky" and "top-0" and "z-50"
    - src/components/layout/Navbar.tsx contains "BSR" text
    - src/components/layout/MobileTabBar.tsx contains "fixed" and "bottom-0"
    - src/components/layout/MobileTabBar.tsx contains "Home" and "ShoppingCart" and "User"
    - src/components/layout/Footer.tsx contains "+91 78293 33444"
    - src/components/layout/Footer.tsx contains "9:00 AM"
    - src/app/layout.tsx contains "Navbar" and "Footer" and "MobileTabBar"
  </acceptance_criteria>
  <done>Navbar with mobile drawer, MobileTabBar with 4 tabs, and Footer with contact info all render correctly</done>
</task>

<task type="auto">
  <name>Task 2: Build HeroBanner carousel and StoreLocator sections</name>
  <files>src/components/sections/HeroBanner.tsx, src/components/sections/StoreLocator.tsx, src/components/sections/WhatsAppFloat.tsx, src/app/page.tsx</files>
  <read_first>src/components/ui/carousel.tsx, src/components/ui/card.tsx, src/app/page.tsx</read_first>
  <action>
    1. Install autoplay plugin: `npm install embla-carousel-autoplay`

    2. Create `src/components/sections/HeroBanner.tsx` as a `'use client'` component:
       - Use Shadcn `Carousel` component with `embla-carousel-autoplay` plugin
       - Configure: loop=true, autoplay delay=5000ms, stopOnInteraction=false, stopOnMouseEnter=true
       - Create 4 slides with gradient backgrounds (brand-red to brand-orange variations):
         - Slide 1: "Summer Collection 2026 — Up to 50% Off" + "Shop Now" CTA button
         - Slide 2: "New Arrivals in Men's Wear" + "Explore" CTA button
         - Slide 3: "All India Home Delivery Available" + "Order Now" CTA button
         - Slide 4: "School & Festival Bulk Orders — Special Discounts" + "Inquire Now" CTA button
       - Each slide: min-height h-[400px] md:h-[500px] lg:h-[600px], centered text with white color, overlay gradient for readability
       - Dot indicators below carousel for current slide
       - First slide image uses `priority` for fast load

    3. Create `src/components/sections/StoreLocator.tsx`:
       - Section title: "Visit Our Stores"
       - Two columns on desktop (grid-cols-2), stacked on mobile
       - Each branch card (using Shadcn Card):
         - Branch name: "BSR Shopping Mall - Sompeta" / "BSR Shopping Mall - Palasa"
         - Address text
         - Phone: clickable `<a href="tel:+917829333444">+91 78293 33444</a>`
         - Hours: "Daily: 9:00 AM – 9:00 PM"
         - Google Maps iframe embed: `<iframe src="https://www.google.com/maps/embed?pb=!1m18!..." width="100%" height="250" style="border:0" allowFullScreen loading="lazy" />`
         - "Get Directions" button linking to Google Maps

    4. Create `src/components/sections/WhatsAppFloat.tsx` as a `'use client'` component:
       - Fixed position: `fixed bottom-6 right-6 z-50` (adjust to `bottom-20` on mobile to avoid overlap with MobileTabBar)
       - Green circle button (#25D366) with white WhatsApp SVG icon
       - Link: `https://wa.me/917829333444?text=Hi%20BSR%20Shopping%20Mall%2C%20I%20would%20like%20to%20inquire%20about%20your%20products.`
       - Pulse animation on idle (CSS `animate-pulse` or custom keyframe)
       - Scale up on hover: `hover:scale-110 transition-transform`
       - `aria-label="Chat on WhatsApp"` for accessibility
       - `target="_blank" rel="noopener noreferrer"`

    5. Update `src/app/page.tsx` to compose the homepage:
       - `<HeroBanner />`
       - Featured categories section (placeholder cards for Men/Women/Kids with brand gradient borders)
       - `<StoreLocator />`
       - Add `<WhatsAppFloat />` to `src/app/layout.tsx` so it appears on all pages
  </action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - src/components/sections/HeroBanner.tsx contains "Carousel" and "Autoplay" and "Summer Collection"
    - src/components/sections/HeroBanner.tsx contains "autoplay" plugin configuration
    - src/components/sections/StoreLocator.tsx contains "iframe" and "google.com/maps"
    - src/components/sections/StoreLocator.tsx contains "tel:+917829333444"
    - src/components/sections/StoreLocator.tsx contains "9:00 AM"
    - src/components/sections/WhatsAppFloat.tsx contains "wa.me/917829333444"
    - src/components/sections/WhatsAppFloat.tsx contains "aria-label"
    - src/app/page.tsx contains "HeroBanner" and "StoreLocator"
  </acceptance_criteria>
  <done>Hero carousel with 4 slides, Store Locator with maps and contact, and WhatsApp float button all working</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] Homepage shows carousel with 4 rotating slides
- [ ] Navbar is sticky on scroll with brand styling
- [ ] Mobile bottom tab bar shows 4 icons
- [ ] Store Locator section shows Google Maps iframes
- [ ] WhatsApp floating button is visible and links correctly
- [ ] Phone number is clickable (tel: link)
- [ ] Footer displays store hours and contact info
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No TypeScript errors
- STORE-01 (Hero Banner + Store Hours) satisfied
- STORE-02 (Branch Locator + Contact info) satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup-storefront-ui/01-02-SUMMARY.md`
</output>
