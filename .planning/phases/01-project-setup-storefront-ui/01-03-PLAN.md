---
phase: 01-project-setup-storefront-ui
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [src/i18n/request.ts, src/i18n/messages/en.json, src/i18n/messages/te.json, src/middleware.ts, src/i18n/routing.ts, src/app/[locale]/layout.tsx, src/app/[locale]/page.tsx, src/components/layout/LanguageToggle.tsx]
autonomous: true
requirements: [STORE-03]

must_haves:
  truths:
    - "User can view the site in English by default"
    - "User can switch to Telugu via the globe dropdown in the navbar"
    - "Navbar labels, hero banner text, and store locator content change when language is toggled"
    - "URL updates to reflect locale (e.g., /te/ for Telugu)"
  artifacts:
    - path: "src/i18n/messages/en.json"
      provides: "English translation strings for Phase 1 UI"
      contains: "BSR Shopping Mall"
    - path: "src/i18n/messages/te.json"
      provides: "Telugu translation strings for Phase 1 UI"
      contains: "బిఎస్ఆర్"
    - path: "src/components/layout/LanguageToggle.tsx"
      provides: "Globe dropdown component for language switching"
      contains: "DropdownMenu"
    - path: "src/middleware.ts"
      provides: "Locale detection and routing middleware"
      contains: "createMiddleware"
  key_links:
    - from: "src/components/layout/LanguageToggle.tsx"
      to: "src/i18n/routing.ts"
      via: "locale switching"
      pattern: "useRouter|usePathname"
    - from: "src/middleware.ts"
      to: "src/i18n/routing.ts"
      via: "routing config"
      pattern: "routing"
---

<objective>
Implement English/Telugu internationalization using next-intl with a globe dropdown toggle in the navbar.

Purpose: Enable the local Telugu-speaking audience to browse the site in their native language, improving accessibility and trust.
Output: Working i18n system with language toggle, English and Telugu translations for all Phase 1 content.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-setup-storefront-ui/01-CONTEXT.md
@.planning/phases/01-project-setup-storefront-ui/01-RESEARCH.md
@.planning/phases/01-project-setup-storefront-ui/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install and configure next-intl with locale routing</name>
  <files>src/i18n/request.ts, src/i18n/routing.ts, src/middleware.ts, src/i18n/messages/en.json, src/i18n/messages/te.json, next.config.ts</files>
  <read_first>next.config.ts, src/app/layout.tsx</read_first>
  <action>
    1. Run `npm install next-intl` to install the i18n library.

    2. Create `src/i18n/routing.ts`:
       ```typescript
       import { defineRouting } from 'next-intl/routing';
       export const routing = defineRouting({
         locales: ['en', 'te'],
         defaultLocale: 'en'
       });
       ```

    3. Create `src/i18n/request.ts`:
       ```typescript
       import { getRequestConfig } from 'next-intl/server';
       import { routing } from './routing';
       export default getRequestConfig(async ({ requestLocale }) => {
         let locale = await requestLocale;
         if (!locale || !routing.locales.includes(locale as any)) {
           locale = routing.defaultLocale;
         }
         return {
           locale,
           messages: (await import(`./messages/${locale}.json`)).default
         };
       });
       ```

    4. Create `src/middleware.ts`:
       ```typescript
       import createMiddleware from 'next-intl/middleware';
       import { routing } from './i18n/routing';
       export default createMiddleware(routing);
       export const config = {
         matcher: ['/', '/(te|en)/:path*']
       };
       ```

    5. Update `next.config.ts` to add next-intl plugin:
       ```typescript
       import createNextIntlPlugin from 'next-intl/plugin';
       const withNextIntl = createNextIntlPlugin('./src/i18n/request.ts');
       export default withNextIntl(nextConfig);
       ```

    6. Create `src/i18n/messages/en.json` with all Phase 1 translatable strings:
       ```json
       {
         "nav": { "men": "Men", "women": "Women", "kids": "Kids", "bulkOrders": "Bulk Orders", "search": "Search", "cart": "Cart", "account": "Account", "home": "Home", "categories": "Categories" },
         "hero": {
           "slide1Title": "Summer Collection 2026", "slide1Subtitle": "Up to 50% Off", "slide1Cta": "Shop Now",
           "slide2Title": "New Arrivals in Men's Wear", "slide2Cta": "Explore",
           "slide3Title": "All India Home Delivery Available", "slide3Cta": "Order Now",
           "slide4Title": "School & Festival Bulk Orders", "slide4Subtitle": "Special Discounts", "slide4Cta": "Inquire Now"
         },
         "storeLocator": { "title": "Visit Our Stores", "sompeta": "BSR Shopping Mall - Sompeta", "palasa": "BSR Shopping Mall - Palasa", "hours": "Daily: 9:00 AM – 9:00 PM", "getDirections": "Get Directions", "phone": "+91 78293 33444" },
         "footer": { "tagline": "BSR Shopping Mall - Baratam Group", "copyright": "© 2026 BSR Shopping Mall. All rights reserved.", "quickLinks": "Quick Links", "contactUs": "Contact Us" },
         "language": { "en": "English", "te": "తెలుగు", "switchLanguage": "Language" }
       }
       ```

    7. Create `src/i18n/messages/te.json` with Telugu translations:
       ```json
       {
         "nav": { "men": "పురుషులు", "women": "మహిళలు", "kids": "పిల్లలు", "bulkOrders": "బల్క్ ఆర్డర్లు", "search": "వెతకండి", "cart": "కార్ట్", "account": "ఖాతా", "home": "హోమ్", "categories": "విభాగాలు" },
         "hero": {
           "slide1Title": "సమ్మర్ కలెక్షన్ 2026", "slide1Subtitle": "50% వరకు తగ్గింపు", "slide1Cta": "ఇప్పుడే షాపింగ్ చేయండి",
           "slide2Title": "పురుషుల దుస్తుల్లో కొత్త రాకలు", "slide2Cta": "అన్వేషించండి",
           "slide3Title": "ఆల్ ఇండియా హోమ్ డెలివరీ అందుబాటులో ఉంది", "slide3Cta": "ఇప్పుడే ఆర్డర్ చేయండి",
           "slide4Title": "స్కూల్ & పండుగ బల్క్ ఆర్డర్లు", "slide4Subtitle": "ప్రత్యేక తగ్గింపులు", "slide4Cta": "ఇప్పుడే విచారించండి"
         },
         "storeLocator": { "title": "మా స్టోర్‌లను సందర్శించండి", "sompeta": "బిఎస్ఆర్ షాపింగ్ మాల్ - సోంపేట", "palasa": "బిఎస్ఆర్ షాపింగ్ మాల్ - పలాస", "hours": "ప్రతిరోజూ: ఉదయం 9:00 – రాత్రి 9:00", "getDirections": "దిశలు పొందండి", "phone": "+91 78293 33444" },
         "footer": { "tagline": "బిఎస్ఆర్ షాపింగ్ మాల్ - బారతం గ్రూప్", "copyright": "© 2026 బిఎస్ఆర్ షాపింగ్ మాల్. అన్ని హక్కులు రిజర్వ్ చేయబడ్డాయి.", "quickLinks": "త్వరిత లింక్‌లు", "contactUs": "మమ్మల్ని సంప్రదించండి" },
         "language": { "en": "English", "te": "తెలుగు", "switchLanguage": "భాష" }
       }
       ```
  </action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - package.json contains "next-intl" dependency
    - src/i18n/routing.ts contains "locales: ['en', 'te']"
    - src/i18n/messages/en.json contains "BSR Shopping Mall"
    - src/i18n/messages/te.json contains "బిఎస్ఆర్"
    - src/middleware.ts contains "createMiddleware"
    - next.config.ts contains "createNextIntlPlugin"
  </acceptance_criteria>
  <done>next-intl configured with locale routing, English and Telugu translation files created</done>
</task>

<task type="auto">
  <name>Task 2: Create LanguageToggle component and wire i18n into existing components</name>
  <files>src/components/layout/LanguageToggle.tsx, src/app/[locale]/layout.tsx, src/app/[locale]/page.tsx</files>
  <read_first>src/components/ui/dropdown-menu.tsx, src/components/layout/Navbar.tsx, src/components/sections/HeroBanner.tsx, src/components/sections/StoreLocator.tsx, src/i18n/routing.ts</read_first>
  <action>
    1. Create `src/components/layout/LanguageToggle.tsx` as a `'use client'` component:
       - Use Shadcn `DropdownMenu` with a globe icon (`Globe` from lucide-react) as the trigger button
       - List 2 options: "English" and "తెలుగు"
       - On selection, use `useRouter` and `usePathname` from `next-intl/navigation` to switch locale
       - Show current locale as active/checked item
       - Import `createNavigation` from `next-intl/navigation` using the routing config

    2. Move existing `src/app/layout.tsx` to `src/app/[locale]/layout.tsx`:
       - Wrap children with `<NextIntlClientProvider messages={messages}>` from next-intl
       - Accept `params: { locale }` and load messages via `getMessages()`
       - Set `<html lang={locale}>` dynamically
       - Import `LanguageToggle` and pass it to `Navbar` as a prop or render it inside

    3. Move existing `src/app/page.tsx` to `src/app/[locale]/page.tsx`:
       - Replace all hardcoded text strings with `useTranslations('namespace')` hooks
       - Use `const t = useTranslations('hero')` in HeroBanner, `useTranslations('storeLocator')` in StoreLocator, etc.
       - Update Navbar to use `useTranslations('nav')` for menu labels
       - Update Footer to use `useTranslations('footer')` for all text

    4. Update Navbar to include the `<LanguageToggle />` component in the right-side icons area (between search and cart icons).

    5. Create a root `src/app/layout.tsx` that simply redirects to the default locale or acts as a passthrough (next-intl handles routing).
  </action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - src/components/layout/LanguageToggle.tsx contains "DropdownMenu" and "Globe"
    - src/components/layout/LanguageToggle.tsx contains "useRouter" or "usePathname"
    - src/app/[locale]/layout.tsx contains "NextIntlClientProvider"
    - src/app/[locale]/layout.tsx contains "getMessages"
    - src/app/[locale]/page.tsx contains "useTranslations"
  </acceptance_criteria>
  <done>Language toggle renders in navbar, switching between English and Telugu updates all UI text</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] Default locale (English) loads at `/` or `/en`
- [ ] Telugu locale loads at `/te/` with translated text
- [ ] Globe dropdown in navbar shows English and Telugu options
- [ ] Switching language updates URL and all visible text
- [ ] No hydration errors in browser console
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No TypeScript errors
- STORE-03 (Language Toggle English/Telugu) satisfied
- All Phase 1 text content translatable
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup-storefront-ui/01-03-SUMMARY.md`
</output>
