---
phase: 01-project-setup-storefront-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, tsconfig.json, tailwind.config.ts, src/app/layout.tsx, src/app/page.tsx, src/app/globals.css, src/lib/utils.ts, src/components/ui/button.tsx, .env.local, .env.example, next.config.ts, components.json]
autonomous: true
requirements: [STORE-01]

must_haves:
  truths:
    - "Next.js dev server starts without errors on localhost:3000"
    - "Homepage renders with BSR brand colors (red/orange gradient)"
    - "Tailwind CSS utility classes work in components"
    - "Shadcn Button component renders correctly"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Root layout with Inter + Outfit fonts, metadata, and global providers"
      contains: "Inter"
    - path: "src/app/globals.css"
      provides: "Global styles with brand color CSS variables"
      contains: "--brand-red"
    - path: "tailwind.config.ts"
      provides: "Extended Tailwind config with BSR brand colors"
      contains: "brand"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "CSS import"
      pattern: "globals.css"
---

<objective>
Scaffold the Next.js 15 application with Tailwind CSS, Shadcn/ui, and the BSR brand design system.

Purpose: Establish the foundational project structure, tooling, and design tokens that all subsequent phases build upon.
Output: A working Next.js app with brand colors, fonts, and base Shadcn components installed.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-setup-storefront-ui/01-CONTEXT.md
@.planning/phases/01-project-setup-storefront-ui/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 15 project with Tailwind CSS</name>
  <files>package.json, tsconfig.json, next.config.ts, tailwind.config.ts, src/app/layout.tsx, src/app/page.tsx, src/app/globals.css, .env.local, .env.example</files>
  <read_first>.planning/phases/01-project-setup-storefront-ui/01-RESEARCH.md</read_first>
  <action>
    1. Run `npx -y create-next-app@latest ./ --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --turbopack --yes` to scaffold the project.
    2. Create `.env.local` with placeholder: `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=placeholder` and `NEXT_PUBLIC_WHATSAPP_NUMBER=917829333444`.
    3. Create `.env.example` mirroring `.env.local` keys with empty values.
    4. Update `next.config.ts` to add image domains config: `images: { remotePatterns: [{ protocol: 'https', hostname: '**' }] }`.
    5. Update `tailwind.config.ts` to extend the color palette with BSR brand colors:
       - `brand: { red: '#DC2626', orange: '#EA580C', 'red-dark': '#B91C1C', 'orange-dark': '#C2410C' }`
       - Add font families: `sans: ['var(--font-inter)', ...defaultTheme.fontFamily.sans]` and `heading: ['var(--font-outfit)']`
    6. Update `src/app/globals.css` to add CSS custom properties:
       - `--brand-red: #DC2626;`
       - `--brand-orange: #EA580C;`
       - `--brand-gradient: linear-gradient(135deg, #DC2626, #EA580C);`
    7. Update `src/app/layout.tsx`:
       - Import Inter and Outfit from `next/font/google`
       - Set metadata: title "BSR Shopping Mall - Readymade Garments | Sompeta & Palasa", description about the store
       - Apply font CSS variables to the `<html>` element
    8. Replace `src/app/page.tsx` with a simple placeholder homepage that displays "BSR Shopping Mall" in the brand gradient font.
  </action>
  <verify>cd . && npm run dev -- --port 3000 & sleep 5 && curl -s http://localhost:3000 | head -20</verify>
  <acceptance_criteria>
    - package.json contains "next" dependency
    - tailwind.config.ts contains "brand" color extension with "'red': '#DC2626'"
    - src/app/globals.css contains "--brand-red: #DC2626"
    - src/app/layout.tsx contains "Inter" and "Outfit" font imports
    - src/app/layout.tsx contains "BSR Shopping Mall" in metadata title
    - .env.example contains "NEXT_PUBLIC_WHATSAPP_NUMBER"
  </acceptance_criteria>
  <done>Next.js app scaffolded with brand design tokens, fonts, and env configuration</done>
</task>

<task type="auto">
  <name>Task 2: Initialize Shadcn/ui and install base components</name>
  <files>components.json, src/lib/utils.ts, src/components/ui/button.tsx</files>
  <read_first>tailwind.config.ts, src/app/globals.css</read_first>
  <action>
    1. Run `npx shadcn@latest init -y` to initialize Shadcn/ui with default settings.
    2. Install base components needed for Phase 1:
       - `npx shadcn@latest add button card sheet dropdown-menu navigation-menu carousel`
    3. Verify `src/lib/utils.ts` exists with the `cn()` utility function.
    4. Verify `components.json` exists with correct aliases pointing to `@/components` and `@/lib`.
  </action>
  <verify>ls src/components/ui/button.tsx && ls src/components/ui/carousel.tsx && ls src/lib/utils.ts</verify>
  <acceptance_criteria>
    - src/lib/utils.ts contains "export function cn"
    - src/components/ui/button.tsx exists and contains "Button"
    - src/components/ui/carousel.tsx exists and contains "Carousel"
    - src/components/ui/sheet.tsx exists
    - src/components/ui/dropdown-menu.tsx exists
    - components.json contains "aliases"
  </acceptance_criteria>
  <done>Shadcn/ui initialized with Button, Card, Sheet, DropdownMenu, NavigationMenu, and Carousel components</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] `npm run dev` starts the dev server on port 3000
- [ ] Homepage renders with Inter font and BSR brand colors
- [ ] Shadcn components importable from `@/components/ui/`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No TypeScript errors or warnings
- Brand color system established in both Tailwind config and CSS variables
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup-storefront-ui/01-01-SUMMARY.md`
</output>
